{
  "name": "@0x-jerry/lib",
  "version": "0.9.4",
  "main": "dist/index.js",
  "types": "src/index.ts",
  "license": "MIT",
  "publishConfig": {
    "registry": "https://registry.npmjs.org"
  },
  "files": [
    "src",
    "dist"
  ],
  "scripts": {
    "test": "jest",
    "test:coverage": "jest --coverage",
    "build": "yarn build:node && yarn build:deno",
    "build:node": "rm -rf dist && tsc -p tsconfig.build.json",
    "build:deno": "rm -rf deno && ts-node scripts/build-deno.ts",
    "release": "glib release"
  },
  "peerDependencies": {
    "@vue/reactivity": "^3.2.2"
  },
  "devDependencies": {
    "@babel/core": "^7.15.0",
    "@babel/plugin-syntax-typescript": "^7.14.5",
    "@exyz/glib": "^0.4.4",
    "@types/fs-extra": "^9.0.12",
    "@types/jest": "^26.0.24",
    "@types/node": "^16.7.8",
    "@vue/reactivity": "^3.2.6",
    "fast-glob": "^3.2.7",
    "fs-extra": "^10.0.0",
    "jest": "^27.1.0",
    "mock-socket": "^9.0.3",
    "ts-jest": "^27.0.5",
    "ts-node": "^10.2.1",
    "typescript": "^4.4.2"
  }
}
